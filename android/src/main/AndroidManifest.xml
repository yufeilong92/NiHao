<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--启动执行-->
        <activity android:name=".activtiy.IndexAcitivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--第二个执行-->
        <activity android:name=".activtiy.MainActivity">

        </activity>
        <!--第三个执行-->
        <activity android:name=".activtiy.SecondActivity">

        </activity>
        <activity android:name=".activtiy.AcitivityCycle">

        </activity>
        <activity android:name=".activtiy.DateActivity">

        </activity>
        <activity android:name=".activtiy.CaptureActivity">

        </activity>
        <activity android:name=".activtiy.SendBroadCastActivity">

        </activity>


        <receiver android:name=".broadcast.MyBroadCastReceiver">

            <intent-filter>
                <action android:name="com.example.android.broadcast.MyBroadCastReceiver">

                </action>
            </intent-filter>
        </receiver>
    </application>
    <uses-permission android:name="android.permission.CAMERA">

    </uses-permission>
    <uses-feature android:name="android.hardware.camera2">

    </uses-feature>

    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS">

    </uses-permission>
    <uses-permission android:name="android.permission.READ_PHONE_STATE">

    </uses-permission>
</manifest>